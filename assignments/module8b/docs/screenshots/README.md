# Module 8B Screenshot Evidence

Save the required inventory screenshot as:

```text
01-inventory-eloquent-page.png
```

Suggested caption:

> Laravel Eloquent inventory page at `http://localhost:8000/inventory`, displaying MySQL records retrieved through the `Item` model and rendered by Blade.

The screenshot should show:

- the Module 8 Assignment 8B heading;
- several inventory items;
- the item, category, quantity, and purchase-date columns;
- the browser URL containing `/inventory`;
- no passwords, `.env` contents, or debugging errors.
