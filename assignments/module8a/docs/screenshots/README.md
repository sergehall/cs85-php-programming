# Module 8A Screenshot Evidence

Add the required screenshot to this folder using the following filename:

```text
01-successful-artisan-migrate.png
```

Suggested caption:

> Successful `php artisan migrate` output showing Laravel creating the application tables in the local `orm_practice_db` MySQL database.

An optional second screenshot can document the interactive connection result:

```text
02-database-connection-query.png
```

Suggested caption:

> Module 8A interactive connection check displaying the active database, MySQL version, local user, server time, and migrated table count returned by a read-only query.
